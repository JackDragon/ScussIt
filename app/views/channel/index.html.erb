<!--
  Bryant Chang
  Renjie Long
  Gangbaolede Li
  Jonathan Lo
  Jonathan Wu
-->
<!-- Javascript -->
<%= javascript_include_tag 'chatroom' %>  

<script type="text/javascript">
  jQuery(document).ready(function($) {
    
    add_active(<%= @channel.id %>)
    
    $(window).bind('beforeunload', function(e){
      delete_active(<%= @channel.id %>);
    });
    // Binding the ENTER KEY
    $(document).bind('keypress', function(element) {
        if(element.keyCode==13){
             $('#send_button').trigger('click');
         }
    });

    id = <%= @channel.id %>
    get_messages(id);
    get_userlist(id);
  });
</script>
<!-- <div class="container" style="float: right">
  <h1><%= Channel.find(@channel.id).name %></h1>
</div> -->

<div class="panel-heading">
  <ul class="nav nav-pills" role="tablist">
    <li class="active"><a href="#">Main: <%= Channel.find(@channel.id).name %></a></li>
    <!-- <li><a href="#">Breaking Bad</a></li> -->
    <!-- <li><a href="#">Lie To Me</a></li> -->
  </ul>
</div>

<div class="container-fluid">
  <div class="row-fluid">
    <div class="span9">
      <div class="chat" id="chatbox">
      </div>
    </div>

    <div class="span3">
      <div class="user_list" id="userbox">

      </div>
    </div>
  </div>
</div>

<div class="container">
<!-- <div class="container" onunload="add_active(<% @channel.id %>)" onload="delete_active( <% @channel.id %>)"> -->
  <input id="message_input" type="text" maxlength="255" style="width: 500px; height: 35px" placeholder="Enter your message here...">
  <button id="send_button" onclick="click_send()" class="btn btn-primary">Send</button>
  <br>
  <br>
  <% if !current_user.nil? %>
    <% if !current_user.favorites.exists?(:channel_id => @channel.id) %>
      <button id=<%= @channel.api_id %> class="btn btn-primary follow" >Follow</button>
    <% else %>
      <button id=<%= @channel.api_id %> class="btn btn-primary follow" >Unfollow</button>
    <% end %>
  <% end %>
</div>
<br>
<br>
<br>




<!-- HTML -->


