<!DOCTYPE html>
<html>
<head lang="en">
  <meta charset="UTF-8">
  <title></title>
</head>
<body>
<div>
    This is room <%  %>
</div>
<table>
  <% @messages.each do |message| %>
      <tr>
        <td><%= message.body %></td>
        <td><%= message.uid %></td>
      </tr>
  <% end %>
</table>
<%= button_to "Follow", {:controller => "channels", :action => "follow", :uid => @user.id, :cid => @room.id }  %>
</body>
</html>